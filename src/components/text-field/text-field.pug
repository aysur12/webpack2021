include ../../components/title-form/title-form
include ../../components/desc-state-input/desc-state-input

mixin text-field(params = {})
  -
    const {
      titleText= "",
      descStateText= "",
      type= "",
      name= "",
      placeholder= "",
      autofocus= false,
      required= false,  
      disabled= false,
      focused= false,
      withDescState= false,
      withArrowButton= false,
    } = params;

    const focusedInput = focused ? "text-field__input_focused" : "";
    const inputAttributes = {
      type: type,
      name: name,
      placeholder: placeholder,
      autofocus: autofocus,
      required: required,
      disabled: disabled,
      withArrow: false
    }

  .text-field
    .text-field__title-form
      +title-form(titleText)
    if withDescState
      +desc-state-input(descStateText)
    input.text-field__input(
      class= `${focusedInput}`)&attributes(inputAttributes)
    if withArrowButton
      .text-field__button